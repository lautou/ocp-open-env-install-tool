apiVersion: v1
kind: Service
metadata:
  name: llamastack-postgresql
  namespace: external-rhoai-db
spec:
  ports:
  - port: 5432
    protocol: TCP
    targetPort: 5432
  selector:
    app: llamastack-postgresql
  type: ClusterIP

apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: llamastack-db
spec:
  ports:
  - name: postgresql
    port: 5432
    protocol: TCP
    targetPort: 5432
  selector:
    app: postgresql
  type: ClusterIP

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cluster-namespace-openshift-config.yaml
  - openshift-config-configmap-insights-config.yaml
  - openshift-config-secret-support.yaml

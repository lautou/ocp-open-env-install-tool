apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-ai-projects
  namespace: rhoai-model-registries
spec:
  podSelector:
    matchLabels:
      app: model-registry
      app.kubernetes.io/name: model-registry
      component: model-registry
  policyTypes:
  - Ingress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          opendatahub.io/dashboard: "true"
    ports:
    - port: 8443
      protocol: TCP

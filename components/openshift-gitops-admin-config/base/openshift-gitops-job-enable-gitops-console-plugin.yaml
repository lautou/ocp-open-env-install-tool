apiVersion: batch/v1
kind: Job
metadata:
  annotations:
    argocd.argoproj.io/sync-options: Force=true
  name: patch-console-gitops-plugin
  namespace: openshift-gitops
spec:
  template:
    metadata:
      name: patch-console-gitops-plugin
    spec:
      containers:
        - name: patch-console
          image: registry.redhat.io/openshift4/ose-cli:latest
          command:
            - /bin/bash
            - '-c'
            - |
              set -e
              echo "Ensuring gitops-plugin is in Console plugins list..."
              if ! oc get console.operator.openshift.io cluster -o jsonpath='{.spec.plugins[*]}' | grep -qw "gitops-plugin"; then
                echo "Adding gitops-plugin to Console..."
                oc patch console.operator.openshift.io cluster --type=json -p='[{"op": "add", "path": "/spec/plugins/-", "value": "gitops-plugin"}]'
                echo "✅ Plugin gitops-plugin added."
              else
                echo "✅ Plugin gitops-plugin already present."
              fi
      nodeSelector:
        node-role.kubernetes.io/infra: ""
      restartPolicy: Never
      serviceAccountName: openshift-gitops-argocd-application-controller
      tolerations:
        - key: node-role.kubernetes.io/infra
          operator: Exists

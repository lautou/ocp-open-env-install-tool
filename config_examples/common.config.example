# ==============================================================================
# COMMON CONFIGURATION (EXAMPLE)
# Shared variables for all profiles.
# Copy this to 'common.config' and customize.
# ==============================================================================

# --- CLUSTER IDENTITY (DEFAULTS) ---
# Default cluster name (can be overridden in specific profiles)
CLUSTER_NAME="myocp"

# --- OPENSHIFT VERSION ---
# The specific version of OpenShift to install.
OPENSHIFT_VERSION="4.20.10"

# Base URL to download the openshift-install and oc binaries
OCP_DOWNLOAD_BASE_URL="https://mirror.openshift.com/pub/openshift-v4/clients/ocp"

# --- INSTALLATION TYPE ---
# "IPI" (Installer Provisioned Infrastructure) or "UPI" (User Provisioned Infrastructure)
INSTALL_TYPE="IPI"

# --- CREDENTIALS ---
# These passwords will be used if creating htpasswd users or standard kubeadmin.
OCP_ADMIN_PASSWORD="XXXXXXXXXXXXXXX"
OCP_NON_ADMIN_PASSWORD="XXXXXXXXXXXXXXX"

# --- DAY 2 GITOPS ---
# Set to "true" to automatically configure ArgoCD and apply Day 2 configs.
ENABLE_DAY2_GITOPS_CONFIG="true"

# --- SOFTWARE PROFILES (DEFAULTS) ---
# Git Branch/Tag to use for ArgoCD (Day 2)
GIT_REPO_REVISION="master"

# --- GIT CREDENTIALS (GLOBAL) ---
# If all your clusters use the same Git repo/credentials, define them here.

# Template for Git Credentials (e.g. for ArgoCD to pull from private repos)
GIT_CREDENTIALS_TEMPLATE_URL=""
GIT_CREDENTIALS_TEMPLATE_TOKEN_NAME=""
GIT_CREDENTIALS_TEMPLATE_TOKEN_SECRET=""

# The specific GitOps Repository
GIT_REPO_URL="https://github.com/lautou/ocp-open-env-install-tool.git"
GIT_REPO_TOKEN_NAME=""
GIT_REPO_TOKEN_SECRET=""
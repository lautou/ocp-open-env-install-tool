kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  annotations:
    argocd.argoproj.io/sync-options: Delete=false
  name: edit-cluster-wide-resources
rules:
  - apiGroups:
      - project.openshift.io
    resources:
      - projects
    verbs:
      - '*'

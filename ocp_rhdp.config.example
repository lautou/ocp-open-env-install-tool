# =================================================================
# == OpenShift Container Platform Environment Configuration
# =================================================================
#
# Instructions:
# 1. Copy this file to 'ocp_rhdp.config'.
# 2. Fill in the variables below.
# 3. The 'ocp_rhdp.config' file is ignored by Git to protect your secrets.
#
# CHECK THAT ALL VARIABLE VALUES ARE ENCLOSED IN DOUBLE QUOTES!
# =================================================================

# --- Installation Parameters ---
# Installation Type: "IPI" (Installer Provisioned Infrastructure) or "UPI" (User Provisioned Infrastructure)
INSTALL_TYPE="IPI"

# OpenShift version (e.g., "4.18.17")
OPENSHIFT_VERSION="4.18.17"

# --- Red Hat Demo Platform (RHDP) Configuration ---
# Your RHDP Lab Top Level Route53 Domain, including the leading dot.
# Example: ".sandbox1234.opentlc.com"
RHDP_TOP_LEVEL_ROUTE53_DOMAIN=".sandboxXXXX.opentlc.com"

# The name for your cluster. This will be prefixed to your domain.
# Example: "myocp" -> myocp.sandboxXXXX.opentlc.com
CLUSTER_NAME="myocp"

# --- AWS Credentials & Region ---
# Credentials for the AWS account provided by the RHDP lab.
AWS_ACCESS_KEY_ID="XXXXXXXXXXXXXXXXXXXX"
AWS_SECRET_ACCESS_KEY="XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"

# The AWS Region where the cluster will be deployed.
# Example: "eu-central-1"
AWS_DEFAULT_REGION="eu-central-1"

# --- Node Sizing and Counts ---
# Instance types for OCP nodes.
AWS_INSTANCE_TYPE_CONTROLPLANE_NODES="m6i.2xlarge"
AWS_INSTANCE_TYPE_COMPUTE_NODES="m6i.4xlarge"
AWS_INSTANCE_TYPE_INFRA_NODES="m6i.2xlarge"
AWS_INSTANCE_TYPE_STORAGE_NODES="m6i.2xlarge"

# Node counts for the cluster.
AWS_WORKERS_COUNT="3"
AWS_INFRA_NODES_COUNT="2"
AWS_STORAGE_NODES_COUNT="3"

# --- Day 2 GitOps Configuration ---
# Set to "true" to automatically deploy Day 2 components (ArgoCD, Logging, Monitoring, etc.)
ENABLE_DAY2_GITOPS_CONFIG="true"

# The Git URL for this repository (ocp-open-env-install-tool).
# This is used by ArgoCD to manage Day 2 components.
GIT_REPO_URL="https://gitlab.consulting.redhat.com/openshift-toolkit/ocp-open-env-install-tool.git"

# --- Git Credentials (Optional) ---
# Provide these only if the Git repository specified in GIT_REPO_URL is private.
# The script will first attempt to discover credentials from an existing ArgoCD instance if these are left empty.
# If you provide a token, use one with 'read_repository' scope.
GIT_REPO_TOKEN_NAME=""
GIT_REPO_TOKEN_SECRET=""

# A URL for a generic Git credential template for ArgoCD.
# This allows ArgoCD to access other repositories (e.g., from other toolkits).
GIT_CREDENTIALS_TEMPLATE_URL="https://gitlab.consulting.redhat.com"
GIT_CREDENTIALS_TEMPLATE_TOKEN_NAME="ocp"
GIT_CREDENTIALS_TEMPLATE_TOKEN_SECRET="XXXXXXXXXXXXXXXXXXXXX"
